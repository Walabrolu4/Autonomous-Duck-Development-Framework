# [PROJECT_NAME]

[One to two sentence description of the project.]

## Quick Start

```
[command to install dependencies]
[command to start dev server]
```

## Project Structure

This project uses the Autonomous Duck Deployment Framework (ADDF).
Key files for AI sessions:

- `AGENTS.md` — AI persona rules and permission levels
- `STATE.md` — Current sprint, status, and blockers
- `DOMAIN.md` — Business logic and entity definitions
- `DECISIONS.md` — Immutable architecture decision ledger
- `COMMANDS.md` — Run, build, test, deploy, and rollback commands

See `docs/architecture.md` for system architecture overview.
